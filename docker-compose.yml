services:
  port:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4001:4001"
    environment:
      NODE_ENV: production
      PORT: 4001
      HOSTNAME: 0.0.0.0
    restart: unless-stopped
    networks:
      - intern-network

networks:
  intern-network:
    external: true